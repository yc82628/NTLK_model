# Steam NLP Pipeline — Dependencies
# Install with: pip install -r requirements.txt
#
# IMPORTANT: Install PyTorch separately FIRST using the command
# from https://pytorch.org/get-started/locally/ for your CUDA version.
# Example for CUDA 12.1 (check your version with: nvidia-smi):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then install everything else below:

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

nltk>=3.8.0
gensim>=4.3.0
vaderSentiment>=3.3.2

# transformers >=4.46 required: earlier versions lack eval_strategy /
# processing_class and will raise the TypeErrors you hit during setup.
transformers>=4.46.0
datasets>=2.14.0
accelerate>=0.24.0
sentence-transformers>=2.2.2

bertopic>=0.16.0
umap-learn>=0.5.4
hdbscan>=0.8.33

kaggle>=1.5.16
python-dotenv>=1.0.0
tqdm>=4.66.0
